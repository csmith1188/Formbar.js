<script src="/socket.io/socket.io.js"></script>

<script defer>
	let socket = io()

	function endBreak() {
		socket.emit('endBreak')
		document.getElementById('breakDialog').close()
	}

	socket.on('break', () => {
		document.getElementById('breakDialog').showModal()
		console.log("BreakSocket")
	})

	socket.on('message', (message) => {
		alert(message)
	})

	socket.on('reload', () => {
		location.reload()
	})
</script>